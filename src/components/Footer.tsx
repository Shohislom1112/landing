import React from "react";
import "@/components/footer.scss";
import { ball, ins, logo, twit, yout } from "@/assets";
import Image from "next/image";

const Footer = () => {
  return (
    <div className="div">
      
    </div>
  );
};

export default Footer;
